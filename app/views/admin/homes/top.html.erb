
<div class="container">
    <h4>注文履歴一覧</h4>
    <div class="row">
        <div class="col-8">
 <table class="table">
  <thead class="thead-light">
    <tr>
        <th scope="col">購入日時</th>
        <th scope="col">購入者</th>
        <th scope="col">注文個数</th>
    </tr>
  </thead>
  <tbody>
<% @order_products.each do |order_product| %>
      <tr>
          <td><%= link_to order_product.created_at.strftime("%Y/%m/%d %H:%M:%S"), admin_order_path(order_product.id) %></td>
          <td><%= order_product.name %></td>
          <td></td>
     </tr>
      <% end %>
  </tbody>
  </table>
  </div>
  </div>
  </div>
  <%= paginate @order_products %>
